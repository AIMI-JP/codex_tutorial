# AGENTS.md

## プロジェクト概要
- 電卓アプリを作るためのリポジトリ。
- 回答やコメントには日本語を用いること。

## 開発プロセス
- 実装は必ずテスト駆動開発（TDD）で進めること。
- 1機能ごとに `Red -> Green -> Refactor` を守ること。
- バグ修正時は、先に再現テストを追加してから修正すること。

## テスト戦略
- 単体テストを最優先し、計算ロジックと状態遷移を網羅すること。
- UI結合テストはボタン操作とキーボード操作の主要フローを対象にすること。
- 受け入れ基準は手動確認だけにせず、可能な限り自動テスト化すること。
- カバレッジ目標は `lines/functions/statements 90%`、`branches 85%` とする。

## 品質ゲート
- マージ前に `lint`、`test`、`typecheck` をすべて通過させること。
- いずれかが失敗している状態でのマージは禁止すること。
- CIでもローカルと同一コマンドを実行し、差異を作らないこと。

## 開発運用
- `main` への直接コミットを禁止し、機能ブランチで作業すること。
- コミットは小さく保ち、原則「1テスト追加 + それを通す最小実装」を1単位とすること。
- コミットメッセージは Conventional Commits（`feat:`, `test:`, `refactor:` など）を推奨する。
- 仕様変更時は実装前に `docs/SPECIFICATION.md` を更新すること。

## コーディング規約
- python: pep8に従うこと。
- TypeScript:
  - `tsconfig` は `strict: true` を前提にし、型エラーを残したままマージしない。
  - `any` は原則禁止。やむを得ない場合のみ、理由をコメントで明示して最小範囲で使う。
  - `as` による型アサーションを多用しない。型ガード/絞り込みで解決できる場合はそちらを優先する。
  - import は ESM の `import` を使い、型のみは `import type` を使う。
  - 比較は `===` / `!==` を使う（`==` / `!=` は禁止）。
  - 関数は小さく保ち、副作用を局所化する。計算ロジックは可能な限り純粋関数で実装する。
  - `null`/`undefined` を考慮した実装を行い、デフォルト値は `||` ではなく `??` を優先する。
  - Lint と Test と Typecheck がすべて成功した状態を完了条件にする。

## 禁止事項
- 秘密情報を追加しない。
- 生成コードにAPIキーを埋め込まない。

## メンテナンスポリシー
- 会話の中で繰り返し指示されたことがある場合は反映を検討すること。
- 冗長だったり、圧縮の余地がある箇所を検討すること。
- 簡潔でありながら密度の濃い文書にすること。
