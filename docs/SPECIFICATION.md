# 電卓アプリ 仕様書（MVP）

## 1. 目的
- 日常的な四則演算を素早く正確に行える単一画面の電卓アプリを提供する。
- 学習用途を考慮し、実装とテストが追いやすいシンプル構成を採用する。

## 2. スコープ
### 2.1 対象（MVP）
- 画面上のボタン入力による計算
- キーボード入力による計算
- 四則演算（`+`, `-`, `×`, `÷`）
- 小数点入力（`.`）
- 符号切り替え（`+/-`）
- パーセント（`%`）
- クリア（`C`, `AC`）
- 1件分の計算式と結果表示

### 2.2 対象外（MVPでは実施しない）
- 計算履歴の保存
- 関数電卓機能（`sin`, `cos`, `log` など）
- 単位変換
- テーマ切り替え
- ユーザー認証/クラウド同期

## 3. 画面仕様
### 3.1 画面一覧
- 画面ID: `CALC-01`
- 画面名: 電卓メイン画面
- 画面数: 1画面

### 3.2 情報設計
- 表示エリア
  - `expressionDisplay`: 計算式（例: `12 +`）
  - `mainDisplay`: 現在値 / 結果 / `Error`
- キーエリア
  - 5行 x 4列グリッド
  - 機能キー / 数字キー / 演算子キー / 実行キー

### 3.3 キー配置
- 1行目: `AC`, `C`, `%`, `÷`
- 2行目: `7`, `8`, `9`, `×`
- 3行目: `4`, `5`, `6`, `-`
- 4行目: `1`, `2`, `3`, `+`
- 5行目: `+/-`, `0`, `.`, `=`

### 3.4 レイアウト
- モバイル（320px〜480px）
  - 上部30%: 表示エリア
  - 下部70%: キーエリア
  - タップ領域44px以上
- デスクトップ（768px以上）
  - 中央寄せカード
  - 最大幅420px

## 4. 機能仕様
### 4.1 入力・演算
- `0`〜`9` 入力可。先頭ゼロは不正連続にならないよう制御する。
- `+`, `-`, `×`, `÷` を選択可能。演算子連打時は最後の演算子を採用する。
- 小数点は1オペランドに1回のみ入力可能。
- `=` で計算結果を表示し、結果は次演算の左辺として再利用する。

### 4.2 `C` / `AC`
- `C`: 現在入力中オペランドのみ `0` に戻す。前値と演算子は保持する。
- `AC`: 状態を完全初期化する（`display=0`, `expression=空`, `operator=null`）。
- UIには `C` と `AC` の両方を配置し、どちらも操作可能とする。

### 4.3 符号・パーセント
- `+/-`: 現在表示中の値の符号を反転する。
- `%`: 現在値を `100` で割った値に変換する。
- `%` は文脈依存計算を行わず、常に単項変換として扱う（例: `200 + 10 %` は `200.1`）。

### 4.4 連続演算
- 複数演算子を含む式は左から逐次計算する（例: `2 + 3 + 4 =` -> `9`）。
- `=` 連打時は、直前の「演算子 + 右オペランド」を再適用する。
  - 例: `2 + 3 =` -> `5`、さらに `=` -> `8`。
- 第2オペランド未入力で `=` が押された場合は、第1オペランドを第2オペランドとして補完する。
  - 例: `2 + =` -> `4`、`5 × =` -> `25`。
- 結果表示後の入力:
  - 数字入力: 新規計算として開始
  - 演算子入力: 継続演算

### 4.5 エラー状態
- `0` 除算または非数値化時は `mainDisplay` に `Error` を表示する。
- `Error` 状態では `AC` のみ受け付ける。
- `AC` 実行で初期状態へ戻る。
- UI表示文言は `Error` に統一する。
- エラー中は `AC` 以外のボタンを非活性表示にする（配置は維持）。

### 4.6 演算子先行入力
- 初期状態または結果表示状態で演算子を押した場合、現在表示値を左オペランドとして演算子を受け付ける。
  - 例: 初期状態で `+` -> `0 +`。
  - 例: `8` 表示中に `×` -> `8 ×`。

## 5. 数値・表示ルール
- 内部計算は JavaScript `number` を使用する。
- 入力可能桁数は最大12桁（符号・小数点を除く）。超過入力は無視する。
- 入力桁数制限は手入力値にのみ適用し、計算結果には適用しない。
- 表示整形は計算後に `toPrecision(12)` 相当で実施し、不要な末尾ゼロを除去する。
- `-0` は `0` として表示する。
- `NaN` / `Infinity` は `Error` として扱う。
- 結果が表示上限を超える場合は指数表記を使用し、表現不能時は `Error` とする。

## 6. キーボード仕様
- 必須対応: `0-9`, `.`, `+`, `-`, `*`, `/`, `Enter`, `Escape`
- 追加対応: `Backspace`
- 正規化: `* -> ×`, `/ -> ÷`
- `Escape` は常に `AC` と同じ動作にマップする。
- IME変換中（`isComposing`）はキー入力を無視する。
- `Backspace` 詳細:
  - 第1/第2オペランド入力中のみ有効
  - 末尾1文字削除
  - 空文字または `-` のみになったら `0` に戻す
  - `=` 直後の結果表示中は無視する

## 7. 状態遷移
- `S0`: 初期状態（`mainDisplay=0`, `expressionDisplay=空`）
- `S1`: 第1オペランド入力中
- `S2`: 演算子選択済み
- `S3`: 第2オペランド入力中
- `S4`: 計算結果表示
- `S5`: エラー状態（`AC` のみ受け付け）

## 8. 非機能要件
- 入力後100ms以内に表示更新
- 最新版 Chrome / Safari / Edge で動作
- レスポンシブ対応（最小幅320px）
- 表示領域とボタン役割が視覚的に明確であること
- キーボード操作可能、フォーカス可視化、コントラストはWCAG AA相当を目標

## 9. 受け入れ基準
- `2 + 3 =` で `5`
- `5 × 6 =` で `30`
- `9 ÷ 0 =` で `Error`（`AC` 押下まで通常入力不可）
- `1 . 2 . 3` で `1.23`
- `12 + 3 = + 4 =` で `19`

## 10. 未確定事項チェックリスト
- [ ] キーイベント実装方針を確定する（`keydown`/`keyup`、`Enter`時のフォーム送信抑止など）。
- [ ] `Error` 表示時の `expressionDisplay` の扱いを確定する（保持/クリア）。
- [ ] 100ms要件の測定条件を確定する（計測方法、対象操作、検証環境）。
- [ ] アクセシビリティ判定条件を確定する（Tab順、読み上げ順、`aria-live` 要否）。
