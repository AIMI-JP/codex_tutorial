# 手動E2E確認手順（MVP電卓）

## 1. 前提
1. `npm install` 済み
2. `npm run dev` でアプリ起動
3. ブラウザで `http://127.0.0.1:5173/` を開く

## 2. 主要シナリオ
1. `2 + 3 =` をクリック入力し、`5` が表示されること
2. `5 × 6 =` をクリック入力し、`30` が表示されること
3. `9 ÷ 0 =` で `Error` が表示され、`AC` 以外が操作不可であること
4. `AC` で初期状態（`0`）に戻ること
5. `1 . 2 . 3` で `1.23` 表示（小数点が重複しない）こと
6. `12 + 3 = + 4 =` で `19` 表示になること

## 3. キーボードシナリオ
1. `4 * 5 Enter` で `20` が表示されること（`* -> ×`）
2. `9 Escape` で `0` に戻ること（`Escape -> AC`）
3. `123` 入力後 `Backspace` で `12` になること

## 4. 連続演算シナリオ
1. `2 + 3 =` の後に `=` を押して `8` になること
2. `5 × =` で `25` になること
3. 初期状態で `+ 3 =` が `3` になること

## 5. 表示・アクセシビリティ確認
1. 式表示（`expressionDisplay`）と結果表示（`mainDisplay`）が更新されること
2. `÷`, `×`, `+/-`, `.` に `aria-label` が設定されていること
3. キーボードフォーカス時にアウトラインが表示されること
4. 画面幅 320px で操作可能なサイズを維持すること
